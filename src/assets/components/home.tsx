import { Box, Container } from "@mui/material";

export default function Home() {
    return <Container>
        <Box display={"flex"} flexGrow={1} bgcolor={"blue"}>
            test
        </Box>
    </Container>
}